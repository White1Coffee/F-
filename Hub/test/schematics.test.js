const test=require('node:test');const assert=require('node:assert/strict');const {decode,varints,transformBlocks}=require('../src/schematics/schematicStore')
test('schematic decoder reads palette, dimensions and materials',()=>{const value=decode({Width:2,Height:1,Length:2,Palette:{'minecraft:air':0,'minecraft:stone':1,'minecraft:oak_planks[axis=y]':2},BlockData:Buffer.from([1,0,2,1])});assert.equal(value.blockCount,3);assert.deepEqual(value.materials,{stone:2,oak_planks:1});assert.deepEqual(value.blocks[1],{x:0,y:0,z:1,name:'oak_planks'})})
test('schematic decoder accepts Sponge v3 Blocks section',()=>{const value=decode({Width:1,Height:1,Length:1,Blocks:{Palette:{'minecraft:dirt':0},Data:Buffer.from([0])}});assert.equal(value.blocks[0].name,'dirt')})
test('schematic varints and rotation are deterministic',()=>{assert.deepEqual(varints(Buffer.from([0,0xac,0x02]),2),[0,300]);assert.deepEqual(transformBlocks([{x:1,y:2,z:0,name:'stone'}],90,3,4),[{x:3,y:2,z:1,name:'stone'}])})
